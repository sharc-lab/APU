"""Microbenchmark drivers.

``attrib`` implements E-ATTRIB (docs/EXPERIMENT_attrib_spec.md): the per-step latency attribution
sweep behind the rotation, the agent-step floor, and the router critique.
"""
